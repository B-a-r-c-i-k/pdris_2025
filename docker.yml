---
- hosts: all
  remote_user: userxd
  become: true
  tasks:
    - name: install dependencies
      apt:
        name: "{{item}}"
        state: present
        update_cache: yes
      loop:
        - apt-transport-https
        - ca-certificates
        - curl
        - gnupg-agent
        - software-properties-common
    - name: add GPG key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present
    - name: add docker repository to apt
      apt_repository:
        repo: deb https://download.docker.com/linux/ubuntu bionic stable
        state: present
    - name: install docker
      apt:
        name: "{{item}}"
        state: latest
        update_cache: yes
      loop:
        - docker-ce
        - docker-ce-cli
        - containerd.io
    - name: check docker is active
      service:
        name: docker
        state: started
        enabled: yes
    - name: Ensure group "docker" exists
      ansible.builtin.group:
        name: docker
        state: present
    - name: adding userxd to docker group
      user:
        name: userxd
        groups: docker
        append: yes
    - name: Install docker-compose
      get_url:
        url: https://github.com/docker/compose/releases/download/1.29.2/docker-compose-Linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: 'u+x,g+x'
    - name: Change file ownership, group and permissions
      ansible.builtin.file:
        path: /usr/local/bin/docker-compose
        owner: userxd
        group: userxd
    - name: Install python3-pip
      apt:
        name: python3-pip
        state: present
        update_cache: yes
    - name: Install python Docker SDK
      pip:
        name: docker==4.4.4
        executable: pip3
    - name: Install docker-compose Python package
      pip:
        name: docker-compose==1.26.0
        executable: pip3
    - name: Copy docker-compose.yml
      copy:
        src: docker-compose.yml
        dest: /home/userxd/docker-compose.yml
        owner: userxd
        group: userxd
        mode: '0644'

    - name: Run docker-compose
      community.docker.docker_compose:
        project_src: /home/userxd/
        state: present
    # - name: pull
    #   community.docker.docker_image_pull:
    #     name: barcikwhyme/lab3-python-project
    # - name: Запустите контейнер Docker 
    #   docker_container: 
    #     name: my_container 
    #     image: barcikwhyme/lab3-python-project 
    #     state: started 